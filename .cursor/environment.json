{
  "name": "AdsPilot (Google-Monetize)",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "aicore",
      "command": "PORT=8090 go run ./services/aicore/",
      "description": "aicore Go microservice (zero external dependencies). Health: GET http://localhost:8090/health (also /healthz, /readyz), Prometheus: /metrics."
    },
    {
      "name": "frontend",
      "command": "cd apps/frontend && npm run dev",
      "description": "Next.js 15 dev server on http://localhost:3000. Public marketing pages work out of the box; add real Supabase keys to apps/frontend/.env.local for authenticated flows."
    }
  ],
  "ports": [
    { "name": "frontend", "port": 3000 },
    { "name": "aicore", "port": 8090 }
  ]
}
