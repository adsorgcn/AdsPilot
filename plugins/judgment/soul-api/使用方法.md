::ILANG::v5.0::ADSPILOT_PLUGIN_SOUL_API
[TYPE:usage][SCOPE:public][LANG:zh][PLUGIN:judgment/soul-api][VERSION:2.0.0][STATUS:alpha]

# 使用方法：托管 SOUL API（公益期）

::STATE{@PLUGIN, id:judgment/soul-api, host:"iLang Inc.", price:0, contract:soul/soul-api.md}

[STEP:1|领密钥|by:human]
公益期在社区领取端点与密钥，放 `SOUL_API_ENDPOINT`、`SOUL_API_KEY`；`judgment.provider` 改为 `soul-api`。

[STEP:2|发前扫描|by:agent]
`provider.py` 发请求前会删掉键名含 token、secret、password、api_key、gclid 的字段。本轮报告里的判断结果会带 `provider:soul-api`。

::RULE{超时8秒或非200⇒退回本地规则 记fallback:true}
::BOUNDARY{never:把凭据_gclid原文_他人数据发出去|scope:permanent}
