::ILANG::v5.0::ADSPILOT_IPINTEL_CONTRACT
[TYPE:contract][SCOPE:public][LANG:zh]

# IP 情报插件契约（待首发）

::STATE{@PLUGIN_KIND, kind:ipintel, first:none, purpose:"过滤假流量 核对目标市场 只做过滤 不做任何伪装"}

## 动作

`lookup`：输入 IP 列表，输出 `{ip, country, is_datacenter, is_proxy, risk?}`。

::BOUNDARY{never:用IP情报做cloaking或按访客身份改内容|scope:permanent}
