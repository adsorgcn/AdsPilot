::ILANG::v5.0::ADSPILOT_PLUGIN_TEMPLATE
[TYPE:usage][SCOPE:public][LANG:zh][PLUGIN:<kind>/<name>][VERSION:0.1.0][STATUS:alpha]

# 使用方法：<插件名>

::STATE{@PLUGIN, id:<kind>/<name>, contract:plugins/<kind>/契约.md}
::OBJECTIVE{id:<id>|owner:user|trust:untrusted|version:1|status:active}
  target: <一句话这个插件让用户的 Agent 能做什么>
  ACCEPT: <契约动作各跑通一次 AND 输出过 schema>
  NON_GOALS: <不做什么>

## 账号前提（本人做）

[STEP:0|账号与凭据|by:human]
<本人真实身份的账号；凭据放哪个环境变量；审核不过怎么办（判停，不换身份）>

## 动作

[STEP:1|<动作一>|by:agent]

```bash
python3 plugins/<kind>/<name>/<脚本>.py --out runs/<id>/<输出>.json
```

<每个动作一段：输入、输出形状（schemas/ 里哪一个）、退出码、没有 API 时的退路>

## 什么算这个插件做完

::EVIDENCE{id:e1|deliverable:<动作>|kind:file|ref:runs/<id>/<输出>.json|verified_by:@TOOL|result:pass}

## 待核对

<实现时按该平台当前文档锁死的几个点>

::BOUNDARY{never:<这个平台上绝不做的事>|scope:permanent}
